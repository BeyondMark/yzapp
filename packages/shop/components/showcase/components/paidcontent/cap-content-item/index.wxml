<a bottomCornerClass="{{isPaid?'':'theme-color'}}" bottomCornerText="{{computed.priceText(content,buyStatus,typeSuffix)}}" bottomTagText="{{computed.priceTagText(content,buyStatus)}}" class="cap-content-item" icon="{{computed.icon(content)}}" statusList="{{computed.statusList(content,isPaid,typeSuffix)}}" subtitle="{{computed.subtitle(content,inColumn)}}" themeClass="{{themeClass}}" thumbnail="{{computed.thumbnailUrl(content)}}" title="{{content.title}}" url="{{computed.contentUrl(content)}}"></a>
