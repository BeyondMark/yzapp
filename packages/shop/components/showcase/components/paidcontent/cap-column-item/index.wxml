<a bottomCornerClass="{{isPaid?'':'theme-color'}}" bottomCornerText="{{computed.priceText(column,buyStatus)}}" bottomTagText="{{computed.priceTagText(column,buyStatus)}}" class="cap-column-item" statusList="{{computed.statusList(column,isPaid)}}" subtitle="{{computed.subtitle(column,isPaid)}}" themeClass="{{themeClass}}" thumbnail="{{computed.thumbnailUrl(column)}}" title="{{column.title}}" url="{{computed.columnUrl(column)}}"></a>
